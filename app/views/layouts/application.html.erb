<!DOCTYPE html>
<html>
  <head>
    <title>Furima42504</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <script src="https://js.pay.jp/v2/pay.js"></script>

  </head>

  <body>
    <%= yield %>

<% if user_signed_in? %>
          <div class="user_nav grid-6">
            <%= link_to "ログアウト", destroy_user_session_path, method: :delete %>
            <%= link_to "test", root_path, class: "post" %>
          </div>
        <% else %>
          <div class="grid-6">
            <%= link_to "ログイン", new_user_session_path, class: "post", data: { turbo: false } %>
            <%= link_to "新規登録", new_user_registration_path, class: "post", data: { turbo: false } %>
          </div>
        <% end %>

  </body>
</html>
